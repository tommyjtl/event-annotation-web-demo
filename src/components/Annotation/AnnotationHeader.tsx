// Import global components and utilities
import { SectionHeader } from "@/components/ui";

export default function AnnotationHeader() {
    return (
        <SectionHeader label="Event Annotation" className="flex-shrink-0" />
    );
}
